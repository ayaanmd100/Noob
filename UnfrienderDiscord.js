
const discord = require(discord.js)
const userid = 732496858945028207
const token = .DISCO.TOKEN.ENV

discord.BlockUser(token, userid)
if success then
console.log("Success. %s L FAISAL")
else
console.log("Failed")
